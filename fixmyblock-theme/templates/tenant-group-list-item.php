<div class="post-list__item tenant-group" data-category="{{category}}" data-region="{{region}}">
    <a href="{{group_info_url}}" class="post-list__item__image">
        <img src="{{image}}" alt="" loading="lazy">
    </a>
    <div class="post-list__item__content">
        <h2 class="post-list__item__title">
            <a href="{{group_info_url}}">{{group_name}}</a>
        </h2>
        <p>{{category}} &bull; {{region}}</p>
        <p>
            <a href="{{group_contact_url}}" class="btn btn-primary mr-2">Contact</a>
            <a href="{{group_info_url}}" class="btn btn-default">More info</a>
        </p>
        <div class="tenant-group__description">{{display_description}}</div>
    </div>
</div>
